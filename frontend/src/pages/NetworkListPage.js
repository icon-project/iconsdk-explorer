import React, { Component } from 'react';
import { NetworkListPageContainer } from '../containers'

class NetworkListPage extends Component {
  render() {
    return (
      <NetworkListPageContainer />
    );
  }
}

export default NetworkListPage;
