import React, { Component } from 'react';
import { BTPListPageContainer } from '../containers'

class BTPListPage extends Component {
  render() {
    return (
      <BTPListPageContainer />
    );
  }
}

export default BTPListPage;
